/**
 * MeshVariant — slow-rotating mesh of radial color washes. Renders
 * as a single blurred div so colors read as ambient atmosphere rather
 * than discrete blobs. CSS-only; see globals.css for the .mesh-variant
 * class and keyframes.
 */
export default function MeshVariant() {
  return <div className="mesh-variant" aria-hidden />;
}
